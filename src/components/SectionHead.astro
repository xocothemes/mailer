---
import Icon from "@/components/ui/Icon.astro";

interface Props {
  title: string;
  id?: string;
  note?: string;
  href?: string;
  linkLabel?: string;
  headingLevel?: "h2" | "h3";
}

const { title, id, note, href, linkLabel = "See all", headingLevel: Heading = "h2" } = Astro.props;
---

<div class="head">
  <div>
    <Heading class="t-h2" id={id}>{title}</Heading>
    {note && <p class="t-meta" style="margin-top:0.4rem">{note}</p>}
  </div>

  {
    href && (
      <a class="arrow-link" href={href}>
        {linkLabel}
        <Icon name="arrow-right" size={15} />
      </a>
    )
  }
</div>
